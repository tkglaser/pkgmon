{
  "name": "pkgmon",
  "version": "0.0.2",
  "description": "Runs npm install when package-lock.json has changed",
  "main": "index.js",
  "bin": {
    "pkgmon": "./bin/app.js"
  },
  "scripts": {
    "start": "npm run build && node ./bin/app.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tkglaser",
  "license": "MIT",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/tkglaser/pkgmon"
  },
  "devDependencies": {
    "@types/node": "^11.10.4",
    "typescript": "^3.3.3333"
  }
}
